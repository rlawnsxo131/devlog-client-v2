import * as React from 'react';
import styled from 'styled-components';

type DefaultTagProps = {};

function DefaultTag(props: DefaultTagProps) {
  return <Block></Block>;
}

const Block = styled.div``;

export default DefaultTag;
